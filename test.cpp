#include <iostream>
using namespace std;
void sort(int arr[], int size) {
  // 还未实现
}
int main() {
  int arr[] = {1, 3, 2, 6, 4, 8};
  int size = sizeof(arr) / sizeof(arr[0]);
  sort(arr, size);
  return 0;
}